{
  "enabledPlugins": {},
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "f=$(jq -r '.tool_input.file_path // empty' 2>/dev/null); case \"$f\" in */precisstudy/src/*.ts) cd \"$CLAUDE_PROJECT_DIR\" && npm run --silent typecheck 2>&1 | tail -25 ;; esac"
          }
        ]
      }
    ]
  }
}
